import React from "react";
import css from "./NotFoundPage.module.css";

const NotFoundPage = () => {
  return <div className={css.title}>Ooops... Page is not found...</div>;
};

export default NotFoundPage;
